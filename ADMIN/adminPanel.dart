import 'package:flutter/material.dart';

class aadmin extends StatelessWidget {
  const aadmin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Changia App'),
      ),
    );
  }
}
